<div class="col-md-6">
  <h2>Изменение пароля</h2>  <!-- <%= resource_name.to_s.humanize %> -->

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render partial: "partials/form_errors", locals: { model: @user } %>

    <div class="form-group">
      <%= fas_icon("envelope fa-lg") %>
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
    </div>

    <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
      <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="form-group">
      <%= fas_icon("unlock-alt fa-lg") %>
      <%= f.label :password %> <i>(Оставьте пустым, если не хотите менять)</i><br />
      <%= f.password_field :password, autocomplete: "new-password", class: "form-control",  placeholder: "Введите новый пароль. #{@minimum_password_length} знаков минимум" %>
      <!--<% if @minimum_password_length %>
        <br />
        <em><%= @minimum_password_length %> characters minimum</em>
      <% end %> -->
    </div>

    <div class="form-group">
      <%= fas_icon("unlock-alt fa-lg") %>
      <%= f.label :password_confirmation %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control",  placeholder: "Введите новый пароль еще раз" %>
    </div>

    <div class="form-group">
      <%= fas_icon("unlock-alt fa-lg") %>
      <%= f.label :current_password %><br />
      <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control",  placeholder: "Введите ваш текущий пароль" %>
    </div>

    <div class="actions">
      <%= f.submit "Обновить", class: "btn btn-primary" %>
    </div>
  <% end %>

  <h3>Удалить мой аккаунт</h3>

  <p>Вам здесь не понравилось? <%= button_to "Удалить мой аккаунт", registration_path(resource_name), class: "btn btn-danger", data: { confirm: "Вы уверены?" }, method: :delete %></p>

  <%= link_to "Назад", :back %>
</div>
